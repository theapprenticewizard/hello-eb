version: 0.2

phases:
  install:
    commands:
      - apt-get update -y
      - apt-get install -y maven
  build:
    commands:
      - mvn clean
      - mvn install
artifacts:
  files:
    - 'Dockerfile'
    - 'target/eb-demo-0.0.1-SNAPSHOT.jar'
